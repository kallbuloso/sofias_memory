"""Interoperability adapters that translate an external document format
into the domain primitives Sofias Memory already validates/persists with --
never a second, parallel validation or persistence path."""
